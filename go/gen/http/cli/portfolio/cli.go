// Code generated by goa v3.20.0, DO NOT EDIT.
//
// portfolio HTTP client CLI support package
//
// Command:
// $ goa gen github.com/reidlai/ta-workspace/modules/portfolio/go/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	portfolioc "github.com/reidlai/ta-workspace/modules/portfolio/go/gen/http/portfolio/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() string {
	return `portfolio (list|summary)
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` portfolio list --user-id "Ut eaque nisi sit nesciunt beatae provident."` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		portfolioFlags = flag.NewFlagSet("portfolio", flag.ContinueOnError)

		portfolioListFlags      = flag.NewFlagSet("list", flag.ExitOnError)
		portfolioListUserIDFlag = portfolioListFlags.String("user-id", "REQUIRED", "")

		portfolioSummaryFlags      = flag.NewFlagSet("summary", flag.ExitOnError)
		portfolioSummaryUserIDFlag = portfolioSummaryFlags.String("user-id", "REQUIRED", "")
	)
	portfolioFlags.Usage = portfolioUsage
	portfolioListFlags.Usage = portfolioListUsage
	portfolioSummaryFlags.Usage = portfolioSummaryUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "portfolio":
			svcf = portfolioFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "portfolio":
			switch epn {
			case "list":
				epf = portfolioListFlags

			case "summary":
				epf = portfolioSummaryFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "portfolio":
			c := portfolioc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
				data, err = portfolioc.BuildListPayload(*portfolioListUserIDFlag)
			case "summary":
				endpoint = c.Summary()
				data, err = portfolioc.BuildSummaryPayload(*portfolioSummaryUserIDFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// portfolioUsage displays the usage of the portfolio command and its
// subcommands.
func portfolioUsage() {
	fmt.Fprintf(os.Stderr, `Provide AI insights
Usage:
    %[1]s [globalflags] portfolio COMMAND [flags]

COMMAND:
    list: List implements list.
    summary: Summary implements summary.

Additional help:
    %[1]s portfolio COMMAND --help
`, os.Args[0])
}
func portfolioListUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] portfolio list -user-id STRING

List implements list.
    -user-id STRING: 

Example:
    %[1]s portfolio list --user-id "Ut eaque nisi sit nesciunt beatae provident."
`, os.Args[0])
}

func portfolioSummaryUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] portfolio summary -user-id STRING

Summary implements summary.
    -user-id STRING: 

Example:
    %[1]s portfolio summary --user-id "Accusamus nulla facere."
`, os.Args[0])
}
