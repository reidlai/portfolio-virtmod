$schema: 'https://moonrepo.dev/schemas/project.json'
id: 'portfolio-svelte'
type: 'library'
stack: 'frontend'

tasks:
  build:
    command: 'svelte-package'
    outputs:
      - 'dist/**'
  
  dev:
    command: 'vite dev'
    local: true
